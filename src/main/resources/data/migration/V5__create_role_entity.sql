create table role_entities (
                       role_id                    serial,
                       role_name                  varchar(50) not null,
                       primary key (role_id)
);